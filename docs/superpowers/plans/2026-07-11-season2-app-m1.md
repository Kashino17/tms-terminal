# Season 2 App-Integration — Meilenstein 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Season-2-UI als umschaltbare zweite UI im Live-Code — Toggle, Foundation, Shell (Dock+Island), Terminal-Kern mit echten Sessions.

**Architecture:** Alles Neue unter `mobile/src/season2/`; drei additive Berührpunkte (settingsStore-Flag, Settings-Row, App-Root-Branch); Daten-Layer 1:1 wiederverwendet (WebSocketService, terminalStore, TerminalView). Design portiert aus dem Liquid-Deck-Mockup.

**Tech Stack:** RN 0.73.6 / Expo SDK 50 (bare), zustand, react-navigation v6; NEU: react-native-reanimated ~3.6, react-native-gesture-handler ~2.14, react-native-svg ~14.1, expo-blur ~12.9.

**Spec:** `docs/superpowers/specs/2026-07-11-season2-app-integration-design.md` (im uppercase Worktree)
**Code-Target:** `/Users/ayysir/Desktop/tms-terminal/mobile` auf Branch `feat/manager-chat-redesign` — VOR JEDEM TASK prüfen: `git -C ~/Desktop/tms-terminal rev-parse --abbrev-ref HEAD` muss `feat/manager-chat-redesign` sein. NIEMALS Branch wechseln.

## Global Constraints

- **Isolations-Garantie:** außerhalb `mobile/src/season2/`, der drei Spec-Berührpunkte und der Dep-Dateien (package.json, package-lock, babel.config.js, ggf. android-Auto-Generate) darf KEIN bestehendes File verändert werden.
- **Git-Disziplin (iCloud-Hang):** NIE `git status`/`git add -A`/`git add .`; committen nur mit expliziten Pfaden + `--no-verify -o <paths>`; bei index.lock 2s warten + retry. Parallel-Jobs ignorieren; nie pull/rebase/reset.
- UI-Strings Deutsch, Code Englisch. Default `seasonTwoEnabled=false`.
- Design-Werte (Farben, Radien, Springs, Icon-Pfade) aus dem Mockup übernehmen: `/Users/ayysir/Desktop/TMS Terminal/mockups/season2/liquid-deck/index.html` (`:root`-Token-Blöcke dark/light, SVG-paths, Motion-Tokens).
- Verify-Standard je Task: `npx tsc --noEmit` (in mobile/) sauber; KEINE Browser-/Geräte-Automation; Geräte-Test macht der User.

### Task 1: Native Deps + Toggle + Root-Branch (+ Season2-Platzhalter)
Files: mobile/package.json (+lock), mobile/babel.config.js, mobile/src/store/settingsStore.ts, mobile/src/screens/SettingsScreen.tsx, mobile/src/App.tsx, NEU mobile/src/season2/SeasonTwoRoot.tsx
- [ ] `npx expo install react-native-reanimated react-native-gesture-handler react-native-svg expo-blur` (SDK-50-Auflösung); babel.config.js: `'react-native-reanimated/plugin'` als LETZTES Plugin; `import 'react-native-gesture-handler'` an App-Entry-Spitze; Root in `GestureHandlerRootView style={{flex:1}}` wrappen (beide Trees).
- [ ] settingsStore: `seasonTwoEnabled:boolean=false` + `setSeasonTwoEnabled` (Muster managerChatRedesignEnabled; persist-Key bleibt 'tms-settings').
- [ ] SettingsScreen: neue Sektion „Design" über der Beta-Sektion — Row „Season 2 (Liquid Glass)" mit Switch + Subtext „Neue Oberfläche — jederzeit umschaltbar" (Markup-Klon Beta-Row 400-427).
- [ ] src/App.tsx:116: `{seasonTwoEnabled ? <SeasonTwoRoot/> : <AppNavigator/>}`; SeasonTwoRoot v0: dunkler Screen „Season 2 — im Aufbau" + Button „Zurück zu Klassisch" (setzt Flag false).
- [ ] Verify: tsc sauber; `./deploy.sh` durchlaufen lassen (APK entsteht); Commit `feat(season2-app): UI-Version-Toggle + native Motion/SVG/Blur-Deps`.

### Task 2: Season2-Foundation (Theme, Icons, Glass, Motion)
Files (alle NEU unter mobile/src/season2/): theme/tokens.ts (dark „Kindle-Grau" + light „Outdoor" aus Mockup-:root; Spacing/Type/Radius-Rampen; ThemeProvider + useS2Theme; Persistenz über EIGENEN AsyncStorage-Key 'tms-s2-theme' innerhalb season2/ — settingsStore bleibt unangetastet), icons.tsx (react-native-svg-Komponenten, Pfade 1:1 aus Mockup: mic, send, plus, grid, list, stack, trash, tools, chevron, server, terminal, manager, cloud, browser, more, sun, moon, signal, prayer-moon, notes, close, arrows), components/GlassSurface.tsx (expo-blur BlurView intensity je Theme + 1px Border + Sheen-Linie; Android-Fallback: semi-transparente Fläche wenn Blur zu teuer — Prop `fallback`), motion/springs.ts (Reanimated-Spring-Configs: snappy/gentle analog Mockup-Kurven; withSpring-Presets).
- [ ] Verify: tsc; Mini-Demo im SeasonTwoRoot (Token-Swatches + 4 Icons + GlassSurface sichtbar, Theme-Umschalt-Button wechselt live). Commit.

### Task 3: Shell — Dock + Dynamic Island + Screen-Gerüst
Files NEU: season2/SeasonTwoRoot.tsx (ersetzt v0), season2/components/Dock.tsx, DynamicIsland.tsx, season2/screens/{TerminalsScreen,PlaceholderBridge}.tsx
- [ ] SeasonTwoRoot: eigener State-Router (kein neuer Navigator nötig — interner Screen-State wie Mockup) mit Screens terminals|(bridges); Hintergrund-Gradient dark/light.
- [ ] Dock: 6 Items (Server, Terminals, Manager, Cloud, Browser, Mehr) als GlassSurface-Pille unten; slidende Aktiv-Pille (Reanimated spring auf translateX/width); Terminals = Season2-Screen; Server/Manager/Cloud/Browser/Mehr = Bridges via `navigation.navigate('ServerList'|'ManagerChat'|'Dashboard'|'Browser'|'Settings')` mit benötigten Params (wsService/host/token vom aktiven Server; ohne aktiven Server → Hinweis-Toast „Erst Server verbinden" bzw. ServerList).
- [ ] DynamicIsland: kompakte zentrierte Glass-Pille (reservierte Top-Zone, überlappt nie): Status-Dot+Label (Verbindungszustand), Latenz (wsService.getRtt() 4s-Ticker, nur bei aktiver Verbindung), Gebets-Countdown (bestehende Prayer-Logik/Service wiederverwenden, read-only). Tap → Morph-Expand (Reanimated: height/width-Spring + Content-Fade) mit: aktive Sessions (Tap→Terminal), Verbindung, nächstes Gebet, Season2-Theme-Toggle, „Zurück zu Klassisch"-Row. Outside-Tap/erneuter Tap → zu.
- [ ] Verify: tsc; Commit.

### Task 4: Terminal-Kern (Liste, echte Sessions)
Files NEU: season2/screens/TerminalsScreen.tsx (voll), season2/components/{SessionCard,TerminalInputRow,ServerPickerSheet}.tsx
- [ ] Server-Auswahl: ohne aktive Verbindung Karte „Server verbinden" → Sheet mit storageService.getServers() → getConnection(serverId).connect(cfg) (Token via storage.service SecureStore); Verbindungsstatus in Island.
- [ ] Session-LISTE: Karten aus useTerminalStore.getTabs(serverId) — Farbtag (aus Palette per Index), Titel, Status/AI-Tool-Chip (tab.aiTool/notificationCount), Chevron. EINE Karte expandiert (Accordion): echtes `<TerminalView sessionId wsService visible>`; andere kollabiert. „+ Neues Terminal" unten: `terminal:create` → addTab (unbegrenzt). Session schließen (Karten-Menü: `terminal:close` + removeTab).
- [ ] Umbenennen: DREIFACH-Tipp auf Titel (600ms-Fenster) → Inline-TextInput → updateTab({title}); Einzel-/Doppel-Tipp expandiert nur.
- [ ] Eingabezeile unter der expandierten Karte: Mono-`$`, TextInput „Befehl eingeben…", Senden-Button → `{type:'terminal:input', sessionId, payload:{data: cmd+'\n'}}`; Mikro-Button vorerst Platzhalter-Toast „Diktat kommt in M2" (ausgegraut ok).
- [ ] Verify: tsc; `./deploy.sh` (APK baut). Commit.

### Task 5: QA + Übergabe
- [ ] Isolations-Audit: `git diff --stat <base>..HEAD` — nur erlaubte Pfade; tsc; deploy.sh-APK vorhanden; Ledger; Übergabe-Checkliste für den User (Update installieren → Klassisch unverändert → Design-Toggle → Season 2 testen: verbinden, Session, benennen, senden, Island, zurückschalten).
